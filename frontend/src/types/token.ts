export interface TokenAPI {
    uuid: string;
    file_name: string;
    x: number;
    y: number;
    scale: number;
}

export interface Token {
    id: string;
    x: number;
    y: number;
    scale: number;
}